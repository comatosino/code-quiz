import { Center, Text } from "@chakra-ui/react";

export const Header: React.FC = (): JSX.Element => {
  return (
    <header>
      <Center w="full" h="10vh">
        <Text>Header</Text>
      </Center>
    </header>
  );
};
